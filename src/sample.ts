/**
 * Sample validation hook test file.
 * This file demonstrates the Enterprise AI-DLC Power workflow.
 */

export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function add(a: number, b: number): number {
  return a + b;
}
